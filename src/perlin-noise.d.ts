declare module "perlin-noise" {
  export function generatePerlinNoise(x: number, y: number): number;
  export function generatePerlinNoise(x: number, y: number, z: number): number;
  // Add other methods or types if needed
}
